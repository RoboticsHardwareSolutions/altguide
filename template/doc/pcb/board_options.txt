# в случае если плата не является типовой то поле type = "custom"
# для этого в репозитории должен содержаться файл board_options_draw.png 
# который иллюстрирует структуру и толщины слоев 


[board_options]
    length = XXX                   # длинна печатной платы по оси X в миллиметрах

    width = XXX                    # ширина печатной платы по оси Y в миллиметрах 

    type = "typical"               # тип печатной платы типовая или кастомная

    layers = X                     # количесвтво слоев

    surface_finish = "XXX"         # покрытие контактных площадок

    pcb_color = "XXX"              # цвет паяльной маски печатной платы

    overlay_color = "xxxxx"        # цвет шелкографической маркировки

    pcb_thickness = x.x            # толщина в милиметрах

    cooper_thickness = xx          # толщина меди в микрометрах

    solder_point = xxxx            # колличество точек пайки на случай ручного монтажа

    drill_file_check = true        # Необязательное поле, по умолчанию true. Если нет
                                   # файла сверловки, то необходимо отключить проверку  					                       
                                   # этого файла. Файл сверловки не гененрирруется
                                   # если нечего сверлить!

    ext_bom = false                # значение true или false в зависимости от того есть ли
                                   # внешние коннекторы
                                   # если внешние разъемы есть то обязательно наличие 
                                   # файла ext_bom.csv с указанием доп фурнитуры 
                                   
